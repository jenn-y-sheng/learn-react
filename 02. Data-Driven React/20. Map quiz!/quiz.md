1. What does the `.map()` array method do?
Returns new array with transformations on each element


2. What do we usually use `.map()` for in React?
Making JSX components from data


3. Critical thinking: why is using `.map()` better than just
   creating the components manually by typing them out?
Don't have data ahead of time
Code is more self sustaining, don't need to change code when more data